<nav>
    <div class="navbar">
      <a href="/">
        <img src="/images/logo.png" alt="Logo" style="height: 30px;width: 30px;" class="logo">
      </a>
      <ul>
        <% navbarLinks.forEach(link => { %>
          <li>
            <a href="<%= link.href %>">
              <img style="height: 30px;width: 30px;" src="<%= link.icon %>" alt="<%= link.text %> Icon">
              <%= link.text %>
            </a>
          </li>
        <% }); %>
      </ul>
    </div>
  </nav>