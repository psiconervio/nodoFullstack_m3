<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Superhéroes</title>
  <link rel="stylesheet" href="/styles.css"> <!-- Agrega tus estilos aquí -->
</head>
<body>
  <h1>Lista de Superhéroes</h1>
  <table border="1">
    <thead>
      <tr>
        <th>ID</th>
        <th>Nombre</th>
        <th>Edad</th>
        <th>Poder</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% superheroes.forEach(superheroe => { %>
        <tr>
          <td><%= superheroe.id %></td>
          <td><%= superheroe.nombre %></td>
          <td><%= superheroe.edad %></td>
          <td><%= superheroe.poder %></td>
          <td>
            <a href="/heroes/<%= superheroe.id %>">Ver</a>
            <a href="/heroes/editar/<%= superheroe.id %>">Editar</a>
            <form action="/heroes/<%= superheroe.id %>?_method=DELETE" method="POST" style="display:inline;">
              <button type="submit">Eliminar</button>
            </form>
          </td>
        </tr>
      <% }); %>
    </tbody>
  </table>
</body>
</html>
